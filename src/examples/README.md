# MCP 클라이언트 예제

이 디렉토리는 MCP (Model Context Protocol) 사용 예제를 포함합니다.

## 📁 파일 설명

### mcp_client_example.py
FastAPI 서버의 MCP 엔드포인트를 호출하는 간단한 클라이언트 예제입니다.

## 🚀 실행 방법

### 1. FastAPI 서버 시작

먼저 FastAPI 서버를 실행합니다:

```bash
# 프로젝트 루트 디렉토리에서
python -m src.main

# 또는
poetry run start
```

서버가 `http://localhost:8001`에서 실행됩니다.

### 2. MCP 클라이언트 예제 실행

새 터미널을 열고 다음 명령을 실행합니다:

```bash
# 프로젝트 루트에서
python -m src.examples.mcp_client_example

# 또는 직접 실행
python src/examples/mcp_client_example.py
```

## 📋 예제에서 수행하는 작업

1. **서버 정보 조회** - MCP 서버의 기본 정보 확인
2. **도구 목록 조회** - 사용 가능한 모든 MCP 도구 나열
3. **계산 도구 테스트** - 덧셈, 곱셈, 나눗셈 수행
4. **텍스트 통계 도구 테스트** - 영문/한글 텍스트 분석
5. **헬스 체크** - 서버 상태 확인

## 📊 예상 출력

```
======================================================================
🚀 MCP Client Example - FastAPI 서버 테스트
======================================================================

📋 MCP 서버 정보 조회
----------------------------------------------------------------------
{
  "success": true,
  "mcp_server": {
    "name": "poetry-demo-mcp-server",
    "version": "1.0.0",
    ...
  }
}

🔧 사용 가능한 도구 목록
----------------------------------------------------------------------
{
  "success": true,
  "tools": [...]
}

🧮 계산 도구 테스트
----------------------------------------------------------------------
덧셈: 10 + 20
{
  "success": true,
  "result": 30
}

✅ 모든 테스트가 성공적으로 완료되었습니다!
```

## 🛠️ 직접 API 호출하기

### cURL 예제

```bash
# 도구 목록 조회
curl http://localhost:8001/mcp/tools

# 계산 수행
curl -X POST http://localhost:8001/mcp/calculate \
  -H "Content-Type: application/json" \
  -d '{"operation": "add", "a": 10, "b": 20}'

# 텍스트 통계
curl -X POST http://localhost:8001/mcp/text-stats \
  -H "Content-Type: application/json" \
  -d '{"text": "Hello World"}'
```

### Python requests 예제

```python
import requests

# 계산
response = requests.post(
    "http://localhost:8001/mcp/calculate",
    json={"operation": "multiply", "a": 5, "b": 6}
)
print(response.json())
```

## 🔗 관련 문서

- [MCP_GUIDE.md](../../MCP_GUIDE.md) - MCP 통합 전체 가이드
- [FastAPI Swagger UI](http://localhost:8001/docs) - 서버 실행 후 API 문서 확인

## ⚠️ 문제 해결

### "서버에 연결할 수 없습니다" 오류

FastAPI 서버가 실행 중인지 확인하세요:

```bash
python -m src.main
```

### 포트 충돌

다른 애플리케이션이 8001 포트를 사용 중이면 `src/main.py`에서 포트를 변경할 수 있습니다.
